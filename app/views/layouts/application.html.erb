<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">

    <!-- Title: static and > 20 chars -->
    <title>NeighborFood - Home-cooked meals and food sharing near you</title>

    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">

    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <!-- ✅ FIXED FAVICON -->
    <!-- Explicitly load favicon.png so Rails does NOT try to load favicon.ico -->
    <%= favicon_link_tag "favicon.png" %>

    <!-- Open Graph basic tags -->
    <meta property="og:site_name" content="NeighborFood">
    <meta property="og:type" content="website">
    <meta property="og:url" content="<%= request.original_url if defined?(request) %>">

    <meta property="og:title" content="NeighborFood - Home-cooked meals and food sharing near you">
    <meta property="og:description"
          content="Find home-cooked meals and groceries near you, claim portions and reduce food waste with NeighborFood.">

    <!-- og:image must be 1200x630 and absolute URL -->
    <meta property="og:image" content="<%= image_url('neighborfood-og.png') %>">
    <meta property="og:image:width" content="1200">
    <meta property="og:image:height" content="630">

    <meta name="twitter:card" content="summary_large_image">
    <meta name="twitter:title" content="NeighborFood - Home-cooked meals and food sharing near you">
    <meta name="twitter:description"
          content="Find home-cooked meals and groceries near you, claim portions and reduce food waste with NeighborFood.">
    <meta name="twitter:image" content="<%= image_url('neighborfood-og.png') %>">

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <%= render "shared/navbar" %>

    <main class="nf-page-wrapper">
      <%= yield %>
    </main>

    <%= render "shared/tabbar" %>
    <%= render "shared/post_fab" %>

    <p class="notice"><%= notice %></p>
    <p class="alert"><%= alert %></p>
  </body>
</html>
